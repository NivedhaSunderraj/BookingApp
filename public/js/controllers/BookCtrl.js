angular.module('BookCtrl', []).controller('BookController', function($rootScope, $scope, $http, $location, $timeout) {

  //console.log( $location.search().rest);
  // console.log(sessionStorage.getItem("cred"));
  $scope.book = function() {

  }
});
